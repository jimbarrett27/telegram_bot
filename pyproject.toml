[tool.poetry]
name = "telegram-bot"
version = "0.1.0"
description = "A bot for interacting with me via telegram"
authors = ["Jim Barrett <jimbarrett27@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
google-cloud-secret-manager = "^2.20.2"
google-cloud-storage = "^2.18.2"
requests = "^2.32.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
